<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:dist="http://schemas.android.com/apk/distribution"
          package="com.example.ejercicioparcial">

    <dist:module dist:instant="true"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name="com.example.ejercicioparcial.gui.activities.MatchInfoActivity">
        </activity>
        <activity android:name="com.example.ejercicioparcial.gui.activities.NewMatchActivity">
        </activity>
        <activity android:name="com.example.ejercicioparcial.gui.activities.TeamActivity">
        </activity>
        <activity android:name="com.example.ejercicioparcial.gui.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>